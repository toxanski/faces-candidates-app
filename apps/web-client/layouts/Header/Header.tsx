import { NavMenu } from '../../components/NavMenu';
import { Logo } from '../../components/Logo';

export function Header() {
    return (
        <header>
            <Logo />
            <NavMenu />
        </header>
    );
}
